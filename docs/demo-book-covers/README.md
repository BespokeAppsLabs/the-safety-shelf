# Demo book covers

These seven WebP files are the durable source assets for the development catalog.

After clearing the linked Convex development deployment, restore the demo books,
content blocks, and missing cover files with:

```bash
yarn seed:demo
```

The command is safe to rerun: existing demo records and covers are skipped.
